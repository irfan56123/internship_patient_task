import React from "react";

const Logo: React.FC = () => (
  <div className="flex items-center justify-center mb-6">
    {/* Replace with <img src="/logo.png" alt="Shedula" className="h-12" /> if logo image is available */}
    <span className="text-3xl font-extrabold text-blue-700 tracking-tight">Shedula</span>
  </div>
);

export default Logo; 